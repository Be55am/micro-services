#you have to rename the file from application.properties to bootstrap.properties to use the spring cloud configration server.
spring.profiles.active=dev
spring.application.name=limits-service
spring.cloud.config.uri=http://localhost:8888
